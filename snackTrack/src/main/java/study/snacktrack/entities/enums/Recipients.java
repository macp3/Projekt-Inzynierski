package study.snacktrack.entities.enums;

/**
 * Enumeration defining the possible target groups for scheduled notifications or administrative broadcasts.
 * This enum allows the application to send tailored messages specifically to different segments of the user base.
 */
public enum Recipients {
    premium,
    non_premium,
    all
}